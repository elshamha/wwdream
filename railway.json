{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["python"]
    }
  },
  "deploy": {
    "startCommand": "export DJANGO_SETTINGS_MODULE=atticus_writer.production_settings && python manage.py migrate --noinput && python create_superuser.py || true && python manage.py collectstatic --noinput || true && gunicorn atticus_writer.wsgi --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}