{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["python"]
    }
  },
  "deploy": {
    "startCommand": "python manage.py migrate --run-syncdb --noinput && python create_superuser.py && python manage.py collectstatic --noinput --no-post-process || true && gunicorn atticus_writer.wsgi --bind 0.0.0.0:$PORT --log-level debug --workers 1",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}